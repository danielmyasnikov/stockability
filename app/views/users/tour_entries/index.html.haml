- content_for :body_class do
  = 'tour-entries'

.page-header
  .pull-right
    = link_to variance_text, users_tour_entries_path(:only_variance => !displays_only_variance?), class: "btn #{toggle_active}"
    = link_to 'Assign', '#', class: "btn btn-primary", id: 'assign-tour'
    = collection_select(:tour, :id, @tour_options, :id, :name, { :prompt => 'Assign  Tour' }, class: 'form-control', id: 'select-tour')
  %h2= 'Tour Entries'
  = link_to 'Adjust Selected', '#', :class => 'btn btn-primary', :id => 'adjust-selected'
  = link_to 'Reject Selected', '#', :class => 'btn btn-danger', :id => 'reject-selected'

.wrapper.wrapper-content.animated.fadeInRight
  .ibox.float-e-margins
    .ibox-title
      %h5 Tour Entries
    .ibox-content
      %table.table.table-hover.table-bordered{ id: 'datatable-simple' }
        %thead
          %tr
            %th.checkboxed= check_box_tag :check_all
            %th Tour
            %th Location
            %th Bin
            %th SKU
            %th Barcode
            %th Stock Level
            %th Quantity
            %th Variance
            %th
        %tbody
          - @tour_entries.each do |tour_entry|
            %tr
              %td= check_box_tag :check_stock_level, nil, nil, data: { stock_level_id: tour_entry.stock_level_id, tour_id: tour_entry.tour_id }, class: 'check'
              %td.tour= link_to truncate(tour_entry.tour_name, length: 12), users_tour_path(tour_entry.tour_id), class: 'label label-default tour_name'
              %td= tour_entry.location_code
              %td= tour_entry.bin_code
              %td= tour_entry.sku
              %td= tour_entry.barcode
              %td.stock_level_qty= tour_entry.sum_stock_level_qty
              %td.quantity= tour_entry.sum_quantity
              - variance = tour_entry.sum_variance
              %td.variance{ class: colour_picker(variance) }= variance
              %td
                .btn-group.btn-group-sm
                  = link_to 'Adjust', adjust_variance_users_tour_entries_path(stock_level_id: tour_entry.stock_level_id, tour_id: tour_entry.tour_id), method: :put, :class => 'btn btn-primary'
                  = link_to 'Entries', users_tour_with_entries_path(stock_level_id: tour_entry.stock_level_id, tour_id: tour_entry.tour_id), :class => 'btn btn-default'
                  = link_to 'Reject', reject_variance_users_tour_entries_path(stock_level_id: tour_entry.stock_level_id, tour_id: tour_entry.tour_id), :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-danger'

.tour_entries{ id: 'page-id' }
