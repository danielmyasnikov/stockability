require "rails_helper"

RSpec.describe "Token management", :type => :request do

  it "creates a Token when the user successfully authenticated" do

  end

  it "responds with 401 when the user authentication fails" do

  end

  it "responds with 403 when an authenticated user tries to access warehouse api" do

  end

end
